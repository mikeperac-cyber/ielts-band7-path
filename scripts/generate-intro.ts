import fs from "fs"; import path from "path"; import OpenAI from "openai"; const openai = new OpenAI({ apiKey: process.env.OPENAI_API_KEY }); async function generate() { const mp3 = await openai.audio.speech.create({ model: "tts-1", voice: "onyx", input: "You now have some time to look at the questions." }); const buffer = Buffer.from(await mp3.arrayBuffer()); fs.writeFileSync(path.join(process.cwd(), "public", "audio", "intro.mp3"), buffer); console.log("Intro generated!"); } generate().catch(console.error);
